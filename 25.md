# Handoff & Session Summary — 2025-08-20

**Scope:** stabilize ED Phase‑2 demo notebook; add Pregnancy‑adapted YEARS pathway; unify guards/paths; verify logging; clean legacy bootstrap/duplicates.

---

## 1) Stable Baseline
- **Notebook (working baseline):** `ed-pipeline-demo-eusemv1.ipynb` (with the extended Phase‑2 calculators cell inserted beneath guards).
- **Alt patched copy (with Pregnancy YEARS):** `ed-pipeline-demo-full-phase-2-ui-running_pregYEARS_20250820T095035Z.ipynb` (kept alongside originals).
- **Guards (top-of-notebook; must run first):**
  ```python
  import os, json, datetime as _dt
  BASE = "/kaggle/working" if os.path.exists("/kaggle/working") else ("/content" if os.path.exists("/content") else "/mnt/data")
  _p = lambda *p: os.path.join(BASE, *p)
  CONFIG = {
      "RUN_PIPELINE": True, "RUN_UI": True, "RUN_MEDS": True, "RUN_SYNTH": False,
      "EQUIPMENT_STATUS_PATH": _p("equipment_status.csv"),
      "EQUIPMENT_MOVES_LOG_PATH": _p("moves_log.csv"),
      "SOP_REGISTRY_PATH": _p("sop_registry.csv"),
      "QR_OUTPUT_DIR": _p("qrs"),
      "EVENT_LOG_PATH": _p("event_log.jsonl"),
  }
  os.makedirs(CONFIG["QR_OUTPUT_DIR"], exist_ok=True)
  os.makedirs(os.path.dirname(CONFIG["EVENT_LOG_PATH"]), exist_ok=True)
  def _append_event(ev: dict):
      ev = {"ts": _dt.datetime.utcnow().isoformat()+"Z", **(ev or {})}
      with open(CONFIG["EVENT_LOG_PATH"], "a", encoding="utf-8") as f:
          f.write(json.dumps(ev, ensure_ascii=False) + "\n")
  ```
- **Event sink:** `CONFIG['EVENT_LOG_PATH']` → Kaggle: `/kaggle/working/event_log.jsonl`.

---

## 2) What changed today
- **Added** a single **Phase‑2 calculators (extended, patched)** cell to the baseline notebook:
  - qSOFA, MEWS, HEART, GRACE (coarse), SOFA (minimal)
  - D‑dimer (age‑adjusted), Troponin Δ
  - Wells‑PE/DVT, PERC, **PESI/sPESI**, **SIRS/Sepsis‑3**, **Marburg**, **GBS**, **Child‑Pugh**
  - Corrected Ca (albumin), Anion gap (± albumin correction)
  - HL7 helper (merges **INR/Bilirubin/Albumin** into Labs JSON)
  - UI panel with HEART sub‑components, PE/PERC flags, Wells‑DVT flags, ascites/encephalopathy
  - One‑liners + JSONL logging
- **Inserted** a new **Pregnancy‑adapted YEARS** cell (additive):
  - YEARS items: DVT signs, hemoptysis, “PE most likely”
  - Threshold: **0 items → 1000 μg/L FEU**, **≥1 item → 500 μg/L FEU**
  - Reuses the Phase‑2 Context/Labs widgets; logs `type: "pregnancy_years"`.
- **Cleaned up**:
  - Removed legacy/broken bootstrap cell (illegal annotation due to stray `"KEY": value` lines).
  - Deleted duplicate UI cells that created multiple `Context JSON` widgets.
  - Rebound `_append_event` to respect current `CONFIG['EVENT_LOG_PATH']`.

---

## 3) Incidents / Bugs and Fixes
- **UI said “Deferred…” / widgets missing** → guards were False or run late.  
  **Fix:** run guards first; rerun UI cells once post‑init only.
- **Logger wrote to `/mnt/data`** or failed to open file.  
  **Fix:** set `EVENT_LOG_PATH` in guards to `/kaggle/working/...`; ensure directory; rebind `_append_event`.
- **SyntaxError: *illegal target for annotation*** in old bootstrap.  
  **Cause:** `"KEY": value` lines outside a dict.  
  **Fix:** replaced with a proper dict literal in the new guards block.
- **Pregnancy YEARS “not applicable (pregnant=false)”** despite manual `ctx = {...}`.  
  **Cause:** handler reads **widget** (`ctx_in.value`), not the standalone variable; multiple widget instances existed.  
  **Fix:** synced all “Context JSON” widgets to `"pregnant": true`; bound YEARS to shared widget instance.
- **Kaggle zombie version running in background**; couldn’t stop.  
  **Status:** support advised **wait 24h** for auto‑kill.  
  **Workaround:** avoid “Save & Run All”; run interactively; keep local/GitHub copies.

---

## 4) Verifications
- **Numeric dict snapshot** (from Phase‑2):  
  `{'qsofa': 3, 'mews': 8, 'heart': 2, 'grace_coarse': 110, 'sofa_min': 7, 'wells_pe': 4.5, 'wells_dvt': 0, 'pesi': 188, 'spesi': 2, 'sirs': 3, 'sepsis3': 1, 'gbs': 3, 'marburg': 1, 'child_pugh': 10}`
- **YEARS (pregnancy) log example:**
  ```json
  {"type":"pregnancy_years","result":{"applies":true,"items_count":1,"d_dimer_ug_l":780,"threshold_ug_l":500,"rule_out":false}}
  ```

---

## 5) Minimal Runbook (essentials only)
1. **Run guards (Cell 1)**.  
2. Run **Phase‑2 calculators** cell (UI shows; if not, run cell once more).  
3. *(Optional)* Run **Pregnancy‑adapted YEARS**, then click **Compute + Log**.  
4. **Tail the sink**:
   ```bash
   tail -n 5 $EVENT_LOG_PATH
   ```

---

## 6) Checklist (today’s status)
- [x] Phase‑2 calculators consolidated in one cell.
- [x] HL7 merge (INR/Bili/Albumin) integrated.
- [x] One‑liners + numeric dict verified.
- [x] **Pregnancy‑adapted YEARS** added and logging.
- [x] Guards unified; logger rebound; old bootstrap removed.
- [x] Duplicate widgets removed; context sync confirmed.
- [x] ICU ETA panel available (manual input; no `nonlocal`).
- [x] Meds/Allergies + OCR panel available under `RUN_MEDS` in demo notebooks.
- [ ] Real ICU data feed (UKE) — **not wired** (manual demo OK).
- [ ] Production write‑back to ORBIS — **out of scope** for demo.
- [ ] Kaggle stuck session — **pending** auto‑kill per support (24h).

---

## 7) Notes on Interface vs Internal
- **Interface constraints:** single notebook, widgets behind `RUN_*` guards, explicit JSONL append sink, no servers/sidecars. Cells capture `CONFIG` at definition time → **guards must run first**.  
- **Internal computation:** all calculators are pure/deterministic; YEARS thresholds apply based on the three items; HL7 parser merges a narrow lab set with unit handling.

---

## 8) Next (optional, not required for demo)
- Add a tiny **CSV‑tick ICU feed** to drive ETA panel live.
- Present **last N events** table in‑notebook (read JSONL → DataFrame) for demo storytelling.
- Keep OCR as **paste‑text** path unless Tesseract is guaranteed on the demo machine.
